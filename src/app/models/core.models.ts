export interface HeaderData {
    nombre: string;
    icon?: string;
    badge?: string;
}

export interface MetasData {
    nombre: string;
    img: string;
    progreso: number;
    objetivo: number;
}